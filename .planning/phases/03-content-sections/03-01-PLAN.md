---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/widgets/NosotrosSection.astro
  - src/components/widgets/ProductosSection.astro
autonomous: true
requirements: [ABOU-01, ABOU-02, ABOU-03, PROD-01, PROD-02, PROD-03, PROD-04]

must_haves:
  truths:
    - "NosotrosSection renders exact corporate text in two-column layout with placeholder image"
    - "ProductosSection renders 7 category cards with Tabler icons and Preline accordion expanding to show product lists"
    - "Brand ornaments appear at low opacity in both sections' backgrounds"
  artifacts:
    - path: "src/components/widgets/NosotrosSection.astro"
      provides: "About section with two-column text+image layout"
      min_lines: 30
    - path: "src/components/widgets/ProductosSection.astro"
      provides: "Products grid with 7 accordion cards"
      min_lines: 80
  key_links:
    - from: "NosotrosSection.astro"
      to: "WidgetWrapper.astro"
      via: "import and wrapping"
      pattern: "import WidgetWrapper"
    - from: "NosotrosSection.astro"
      to: "BrandOrnament.astro"
      via: "bg slot"
      pattern: "BrandOrnament"
    - from: "ProductosSection.astro"
      to: "Preline hs-accordion"
      via: "data attributes"
      pattern: "hs-accordion"
---

<objective>
Create the NosotrosSection and ProductosSection widget components -- the two most complex content sections requiring two-column layout and Preline accordion respectively.

Purpose: These are the heaviest content sections in Phase 3. Nosotros establishes who campivacorp. is, Productos showcases the full product catalog with expandable categories.
Output: Two standalone .astro widget components ready to be imported into index.astro.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@.planning/phases/03-content-sections/03-RESEARCH.md

<interfaces>
<!-- WidgetWrapper.astro props -->
From src/components/ui/WidgetWrapper.astro:
```typescript
export interface Props extends Widget {
  containerClass?: string;
  ['as']?: HTMLTag;
}
// Usage: <WidgetWrapper id="nosotros" containerClass="max-w-7xl mx-auto">
//   <Fragment slot="bg">...</Fragment>
//   <!-- content in default slot -->
// </WidgetWrapper>
```

From src/components/ui/Headline.astro:
```typescript
// Props: title, subtitle, tagline, classes: { container, title, subtitle }
// Usage: <Headline title="Quienes Somos" />
```

From src/components/ui/BrandOrnament.astro:
```typescript
export interface Props {
  position?: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right';
  size?: number;
  class?: string;
}
// Usage: <BrandOrnament position="top-right" size={400} />
// ONLY on white-bg sections. Parent needs position: relative.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NosotrosSection.astro widget</name>
  <files>src/components/widgets/NosotrosSection.astro</files>
  <action>
Create `src/components/widgets/NosotrosSection.astro` with:

1. **Imports:** WidgetWrapper, Headline, BrandOrnament from ~/components/ui/

2. **Structure:** WidgetWrapper with id="nosotros"
   - bg slot: white background + BrandOrnament position="top-right" size={400} + BrandOrnament position="bottom-left" size={350}
   - Headline with title="Quienes Somos"
   - Two-column layout using `md:flex md:gap-16`

3. **Left column (md:basis-1/2):** The exact corporate text (copy verbatim from PROJECT.md):
   "Somos una empresa cuyos fundadores tienen mas de 25 anos de experiencia en la industria de oleos, derivados y subproductos; nutricion humana con productos agroindustriales y nutricion animal. Enfocados en brindar soluciones de alimentacion humana y animal, con transacciones tanto locales como internacionales, atendiendo diversos mercados de la region. Nuestros socios estrategicos y nosotros cumplimos con los estandares mas exigentes de inocuidad, calidad, trazabilidad y medio ambiente; monitoreando a todos los participantes de nuestra cadena de valor desde la produccion en campo, transformacion industrial, almacenamiento y logistica, centrando al productor primario y el cliente final como el centro de nuestra estrategia corporativa."

   Style: `text-lg text-muted leading-relaxed`

4. **Right column (md:basis-1/2):** Placeholder image block
   - `w-full h-64 md:h-80 rounded-lg bg-[#95b444]/20 flex items-center justify-center`
   - Inner text: "Imagen" in `text-[#95b444]/60 text-lg font-medium`
   - Structured so a real image can replace the div later

5. **Responsive:** Stacked on mobile (text above image), side-by-side on md+

6. **DO NOT** add AOS attributes yet -- Phase 4 handles animation polish.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>NosotrosSection.astro exists, builds without error, contains exact corporate text, uses WidgetWrapper+Headline+BrandOrnament, two-column layout with placeholder image</done>
</task>

<task type="auto">
  <name>Task 2: Create ProductosSection.astro widget</name>
  <files>src/components/widgets/ProductosSection.astro</files>
  <action>
Create `src/components/widgets/ProductosSection.astro` with:

1. **Imports:** WidgetWrapper, Headline, BrandOrnament from ~/components/ui/ and Icon from 'astro-icon/components'

2. **Data array:** Define products array in frontmatter with all 7 categories. Each entry has: id (slug), name, icon (Tabler name), products (string array). Use these exact icons and product lists:
   - { id: 'aceites', name: 'Aceites', icon: 'tabler:droplet', products: ['Crudo de Soya', 'Crudo de Girasol', 'Refinado de Soya', 'Refinado de Girasol', 'Refinado Soya+Girasol Blend'] }
   - { id: 'harinas', name: 'Harinas y Tortas', icon: 'tabler:grain', products: ['Harina de Soya Expeller', 'Torta de Soya Estandar', 'Torta de Soya HiPro', 'Torta de Girasol', 'Harina de Carne y Hueso', 'Harina de Plumas'] }
   - { id: 'granos', name: 'Granos', icon: 'tabler:plant', products: ['Soya', 'Maiz', 'Sorgo', 'Mani', 'Quinua (blanca/negra/roja)', 'Chia', 'Sesamo (negro/blanco)', 'Cacao', 'Cafe'] }
   - { id: 'endulzantes', name: 'Endulzantes', icon: 'tabler:candy', products: ['Azucar Refinada', 'Azucar Rubia', 'Miel de Abeja', 'Stevia'] }
   - { id: 'grasas', name: 'Grasas', icon: 'tabler:flask', products: ['Mantecas y Margarinas', 'Sebo Bovino', 'Acidos Grasos', 'Oleinas Vegetales', 'Lecitina de Soya'] }
   - { id: 'forestales', name: 'Derivados Forestales', icon: 'tabler:tree', products: ['Carbon Vegetal', 'Lena'] }
   - { id: 'hidrocarburos', name: 'Hidrocarburos', icon: 'tabler:gas-station', products: ['Gasolina', 'Diesel ULSD'] }

3. **Structure:** WidgetWrapper with id="productos"
   - bg slot: white background + BrandOrnament position="bottom-right" size={350}
   - Headline with title="Productos" and subtitle="7 categorias de productos agroindustriales"

4. **Card grid:** Responsive grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
   - For the last row centering (7 items, 3 cols = 1 orphan), wrap in a flex container: the grid renders all 7 cards, and on lg the last item will naturally sit in column 1. To center it, use this approach: wrap the entire grid in a div and use `[&>*:last-child]:lg:col-start-2` on the grid OR use a flexbox approach with `flex flex-wrap justify-center gap-6` with fixed-width cards `w-full md:w-[calc(50%-12px)] lg:w-[calc(33.333%-16px)]`. Choose whichever builds cleanly.

5. **Each card:** Preline accordion pattern with UNIQUE ids per category.
   ```html
   <div class="hs-accordion-group">
     <div class="hs-accordion bg-white rounded-lg border border-[#95b444]/20 shadow-sm hover:shadow-md transition-shadow" id="hs-productos-{id}">
       <button class="hs-accordion-toggle py-4 px-5 inline-flex items-center gap-x-3 w-full font-semibold text-start text-heading" aria-expanded="false" aria-controls="hs-collapse-{id}">
         <Icon name="{icon}" class="w-8 h-8 text-[#95b444] flex-shrink-0" />
         <span class="font-heading text-lg">{name}</span>
         <Icon name="tabler:chevron-down" class="hs-accordion-active:rotate-180 transition-transform w-5 h-5 ml-auto text-muted" />
       </button>
       <div id="hs-collapse-{id}" class="hs-accordion-content hidden w-full overflow-hidden transition-[height] duration-300" role="region" aria-labelledby="hs-productos-{id}">
         <ul class="px-5 pb-4 space-y-1 text-muted text-sm">
           {products.map(p => <li>{p}</li>)}
         </ul>
       </div>
     </div>
   </div>
   ```

6. **CRITICAL:** Every accordion id must be unique. Pattern: `hs-productos-{category.id}` for the accordion, `hs-collapse-{category.id}` for the content panel. The `aria-controls` on button must match the content panel id. The `aria-labelledby` on content must match the accordion id.

7. **If `tabler:candy` icon does not exist** (medium confidence on this one), fall back to `tabler:lollipop` or `tabler:honey`. The build will fail immediately if an icon name is invalid -- fix on first build attempt.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ProductosSection.astro exists, builds without error, contains 7 category cards with correct Tabler icons, each card has Preline accordion with unique ids and full product lists, responsive grid layout, brand ornament in background</done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors (validates icon names, Astro syntax, imports)
- Both .astro files exist in src/components/widgets/
- NosotrosSection contains exact corporate text from PROJECT.md
- ProductosSection has 7 cards with 7 unique accordion id sets
</verification>

<success_criteria>
- NosotrosSection.astro renders two-column layout with exact corporate text and placeholder image
- ProductosSection.astro renders 7 product category cards with Tabler icons and Preline accordion
- Both use WidgetWrapper + Headline + BrandOrnament correctly
- Build passes (no broken imports or invalid icon names)
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>
