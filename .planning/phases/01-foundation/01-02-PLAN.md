---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Logo.astro
  - src/components/ui/BrandOrnament.astro
autonomous: false
requirements: [FOUN-03, FOUN-07]

must_haves:
  truths:
    - "The campivacorp. SVG isotipo (3 overlapping leaves) renders in the navbar header"
    - "The wordmark shows 'campiva' in bold + 'corp.' in regular weight next to the isotipo"
    - "BrandOrnament component renders a large leaf shape at 5-8% opacity when placed in a section"
  artifacts:
    - path: "src/components/Logo.astro"
      provides: "SVG isotipo + wordmark replacing AstroWind emoji logo"
      min_lines: 20
    - path: "src/components/ui/BrandOrnament.astro"
      provides: "Reusable decorative leaf shape for white-background sections"
      min_lines: 15
  key_links:
    - from: "src/components/Logo.astro"
      to: "src/components/widgets/Header.astro"
      via: "import Logo from '~/components/Logo.astro' (already wired)"
      pattern: "import.*Logo"
    - from: "src/components/ui/BrandOrnament.astro"
      to: "future section components"
      via: "will be imported in Phase 3 sections"
      pattern: "BrandOrnament"
---

<objective>
Create the campivacorp. SVG isotipo logo component and reusable brand ornament decoration component.

Purpose: The logo is the most visible brand element -- it appears in the navbar and will be used throughout the site. The ornament component provides subtle leaf-shape background decoration for white-background sections in Phases 2-4.
Output: Logo.astro (SVG isotipo + wordmark) and BrandOrnament.astro (decorative leaf at low opacity).
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Logo.astro is imported in Header.astro -- must maintain same interface (no props, renders inline) -->

From src/components/widgets/Header.astro (line 3, 75):
```astro
import Logo from '~/components/Logo.astro';
// ... used as:
<Logo />
```

Current Logo.astro renders a <span> with emoji + site name. New Logo.astro must also render a <span> (or similar inline element) -- no props needed.

BrandOrnament.astro is NEW -- no existing interface to match. Will be consumed in Phase 3 sections.
Props interface (from RESEARCH.md):
```typescript
export interface Props {
  position?: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right';
  size?: number; // px, default 400
  class?: string;
}
```

Brand colors available after Plan 01:
- #95b444 (primary green) -- lateral leaves, ornament fill
- #5d6f31 (dark green) -- central leaf
- #25272f (carbon) -- leaf border, wordmark text
- #cbdc53 (lime accent) -- not used in logo/ornament
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Logo.astro with SVG isotipo + wordmark</name>
  <files>src/components/Logo.astro</files>
  <action>
REWRITE src/components/Logo.astro completely. Replace the emoji + text span with:

1. A `<span class="flex items-center">` wrapper containing:
   a. An inline SVG isotipo (viewBox="0 0 48 48", class="h-10 w-10") with:
      - 3 organic leaf shapes that overlap (the campivacorp. logo reference):
        - Central leaf: taller, vertical orientation, fill="#5d6f31" (dark green), stroke="#25272f" stroke-width="0.5"
        - Left leaf: angled left ~30deg, fill="#95b444" (primary green), stroke="#25272f" stroke-width="0.5"
        - Right leaf: angled right ~30deg (mirror of left), fill="#95b444", stroke="#25272f" stroke-width="0.5"
      - Leaves should be organic/rounded (use cubic bezier curves, not geometric ellipses)
      - Small stem/union point at the base where leaves converge
      - Set aria-hidden="true" on the SVG
   b. A text span with ml-2: `<span class="font-heading font-extrabold text-[#25272f]">campiva</span><span class="font-heading font-normal text-[#25272f]">corp.</span>`
      - The period after "corp" is part of the brand name and must be included
      - text-xl size for the wordmark

2. Remove the import of SITE from 'astrowind:config' -- no longer needed.

3. SVG crafting guidance: Start with 3 elongated elliptical leaf shapes. The central leaf is straight vertical (pointing up). The two lateral leaves angle outward from the same base point. Each leaf should have a slight taper (wider in middle, pointed at tip and base). Use `<path>` elements with cubic bezier curves for organic feel. The three leaves should overlap slightly in their lower portions.

Note: The exact SVG path coordinates are at Claude's discretion per user decision. Aim for a clean, professional look that matches the reference description. The SVG can be refined later.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Logo.astro renders an inline SVG with 3 overlapping green leaf shapes + "campivacorp." wordmark. Build succeeds. The logo appears wherever Header.astro renders it.</done>
</task>

<task type="auto">
  <name>Task 2: Create BrandOrnament.astro decorative component</name>
  <files>src/components/ui/BrandOrnament.astro</files>
  <action>
CREATE src/components/ui/BrandOrnament.astro (new file, ensure src/components/ui/ directory exists).

1. Component accepts props:
   - `position`: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' (default: 'top-right')
   - `size`: number in px (default: 400)
   - `class`: string for additional classes

2. Renders an absolutely-positioned container with:
   - `absolute pointer-events-none overflow-hidden` base classes
   - Opacity set to `opacity-[0.06]` (6% -- within the 5-8% range from user decision)
   - Position classes mapping:
     - top-left: `top-0 left-0 -translate-x-1/4 -translate-y-1/4`
     - top-right: `top-0 right-0 translate-x-1/4 -translate-y-1/4`
     - bottom-left: `bottom-0 left-0 -translate-x-1/4 translate-y-1/4`
     - bottom-right: `bottom-0 right-0 translate-x-1/4 translate-y-1/4`
   - Width and height set via inline style from `size` prop
   - aria-hidden="true"

3. Inside the container, an SVG (viewBox="0 0 200 200") containing a single large leaf shape:
   - Derived from one of the isotipo leaves, scaled up
   - Fill: #95b444 (primary green)
   - No stroke (the low opacity makes details unnecessary)
   - Simple organic leaf shape -- one `<path>` element with cubic bezier curves

4. The parent section using this component MUST have `position: relative` (or Tailwind `relative` class) for absolute positioning to work. Add a comment in the component file noting this requirement.

Per user decision: Only use on white-background sections. NOT on dark (#25272f) sections.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>BrandOrnament.astro exists in src/components/ui/ with position/size/class props. Renders a low-opacity leaf shape SVG. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1 foundation</name>
  <action>
Human verifies the complete Phase 1 foundation visually: brand colors (green palette), fonts (Nunito Sans + Montserrat), SVG logo with 3 leaves + "campivacorp." wordmark, config (Spanish, blog disabled, light-only), DaisyUI removed, blog routes deleted, and brand ornament component.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms: green palette visible, correct fonts, logo renders properly, no AstroWind blue/purple remnants, browser tab says "campivacorp.", /blog returns 404, text selection is lime green.</done>
  <what-built>Complete Phase 1 foundation: brand colors (green palette), fonts (Nunito Sans + Montserrat), SVG logo with 3 leaves + "campivacorp." wordmark, config (Spanish, blog disabled, light-only), DaisyUI removed, blog routes deleted, and brand ornament component.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the site in browser
    2. Check the navbar: the campivacorp. logo should show 3 green leaf shapes + "campiva" bold + "corp." regular text
    3. Check the browser tab title: should say "campivacorp."
    4. Check headings: should render in Nunito Sans (rounder letterforms than Inter)
    5. Check body text: should render in Montserrat
    6. Open DevTools console: should have NO DaisyUI-related errors
    7. Check colors: NO blue or purple anywhere -- only green palette (#95b444, #5d6f31, #cbdc53) and carbon (#25272f)
    8. Navigate to /blog: should return 404
    9. Select some text on the page: selection highlight should be lime green (#cbdc53)
  </how-to-verify>
  <resume-signal>Type "approved" if the foundation looks correct, or describe any issues with the logo, colors, fonts, or config.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Logo.astro contains inline SVG with leaf paths and "campivacorp." wordmark
3. BrandOrnament.astro exists in src/components/ui/ with position props
4. Visual inspection confirms: green palette, correct fonts, logo rendering, no AstroWind remnants
</verification>

<success_criteria>
- Logo.astro renders 3 overlapping leaf SVG + "campiva" bold + "corp." regular wordmark
- BrandOrnament.astro renders a large leaf at ~6% opacity with configurable position
- Human verifies the visual foundation is correct (checkpoint)
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
