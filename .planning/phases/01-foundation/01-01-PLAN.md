---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.js
  - src/components/CustomStyles.astro
  - src/config.yaml
  - src/pages/[...blog]/index.astro
  - src/pages/[...blog]/[...page].astro
  - src/pages/[...blog]/[category]/
  - src/pages/[...blog]/[tag]/
autonomous: true
requirements: [FOUN-01, FOUN-02, FOUN-04, FOUN-05, FOUN-06]

must_haves:
  truths:
    - "npm run dev shows green palette (#95b444 primary) -- no AstroWind blue/purple anywhere"
    - "Headings render in Nunito Sans 800, body text in Montserrat 500/700 -- Inter is gone"
    - "Browser tab shows 'campivacorp.' as site title"
    - "No DaisyUI console errors, no daisyui in node_modules"
    - "Navigating to /blog returns 404 (routes deleted)"
  artifacts:
    - path: "src/components/CustomStyles.astro"
      provides: "Brand CSS variables + font imports"
      contains: "rgb(149 180 68)"
    - path: "tailwind.config.js"
      provides: "Tailwind config without DaisyUI"
      contains: "typographyPlugin"
    - path: "src/config.yaml"
      provides: "Site metadata in Spanish, blog disabled"
      contains: "campivacorp."
  key_links:
    - from: "src/components/CustomStyles.astro"
      to: "tailwind.config.js"
      via: "CSS variables consumed by Tailwind extend.colors"
      pattern: "var\\(--aw-color"
    - from: "src/config.yaml"
      to: "src/layouts/Layout.astro"
      via: "SITE config import"
      pattern: "apps.*blog.*isEnabled.*false"
---

<objective>
Remove DaisyUI, install brand fonts, rewrite CSS variables to campivacorp. green palette, update site config to Spanish with blog disabled, and delete blog route files.

Purpose: Every subsequent component must inherit correct colors, fonts, and site metadata. This plan eliminates the broken DaisyUI dependency and replaces all AstroWind default branding with campivacorp. tokens.
Output: A clean, building Astro project with campivacorp. brand tokens flowing through the CSS variable system.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Current CSS variable flow: CustomStyles.astro defines :root vars, tailwind.config.js maps them -->

From tailwind.config.js (color/font mappings -- DO NOT change these, only change CustomStyles.astro values):
```javascript
colors: {
  primary: 'var(--aw-color-primary)',
  secondary: 'var(--aw-color-secondary)',
  accent: 'var(--aw-color-accent)',
  default: 'var(--aw-color-text-default)',
  muted: 'var(--aw-color-text-muted)',
},
fontFamily: {
  sans: ['var(--aw-font-sans, ui-sans-serif)', ...defaultTheme.fontFamily.sans],
  serif: ['var(--aw-font-serif, ui-serif)', ...defaultTheme.fontFamily.serif],
  heading: ['var(--aw-font-heading, ui-sans-serif)', ...defaultTheme.fontFamily.sans],
},
```

From src/config.yaml (current AstroWind defaults to replace):
- site.name: "AstroWind" -> "campivacorp."
- i18n.language: "en" -> "es"
- ui.theme: "system" -> "light:only"
- apps.blog.isEnabled: true -> false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove DaisyUI, install brand fonts, clean tailwind.config.js</name>
  <files>package.json, tailwind.config.js</files>
  <action>
1. Run `npm uninstall daisyui @fontsource-variable/inter` to remove broken DaisyUI and old font.
2. Run `npm install @fontsource/nunito-sans @fontsource/montserrat` to add brand fonts.
3. Edit tailwind.config.js:
   - Remove `import daisyui from 'daisyui';` (line 4)
   - Remove `daisyui,` from the plugins array (line 43)
   - Remove the entire `daisyui: { themes: ['light', 'dark'], prefix: 'daisy-' }` config block (lines 45-48)
   - Keep everything else unchanged (colors, fontFamily, animation, keyframes, typographyPlugin, intersect variant, darkMode)
4. Verify no `daisy-` class references exist in src/ (grep confirms zero -- already verified).

Anti-pattern: Do NOT change the color/fontFamily mappings in tailwind.config.js theme.extend. They reference CSS variables that will be updated in Task 2.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DaisyUI gone from package.json and tailwind.config.js. @fontsource/nunito-sans and @fontsource/montserrat in dependencies. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CustomStyles.astro + config.yaml + delete blog routes</name>
  <files>src/components/CustomStyles.astro, src/config.yaml, src/pages/[...blog]/</files>
  <action>
1. REWRITE src/components/CustomStyles.astro completely:
   - Frontmatter: Replace `import '@fontsource-variable/inter'` with three imports:
     ```
     import '@fontsource/nunito-sans/800.css';
     import '@fontsource/montserrat/500.css';
     import '@fontsource/montserrat/700.css';
     ```
   - Remove all commented font names.
   - `:root` block with these exact values (rgb space-separated format, no commas):
     ```css
     --aw-font-sans: 'Montserrat';
     --aw-font-serif: 'Montserrat';
     --aw-font-heading: 'Nunito Sans';
     --aw-color-primary: rgb(149 180 68);
     --aw-color-secondary: rgb(93 111 49);
     --aw-color-accent: rgb(203 220 83);
     --aw-color-text-heading: rgb(37 39 47);
     --aw-color-text-default: rgb(37 39 47);
     --aw-color-text-muted: rgb(37 39 47 / 66%);
     --aw-color-bg-page: rgb(255 255 255);
     --aw-color-bg-page-dark: rgb(37 39 47);
     ```
   - Move `::selection` OUTSIDE `:root` (not nested -- plain CSS has no nesting). Set `background-color: rgb(203 220 83);` (lime green accent).
   - Remove the entire `.dark { ... }` block. No dark mode.

2. REWRITE src/config.yaml:
   ```yaml
   site:
     name: 'campivacorp.'
     site: 'https://campivacorp.com'
     base: '/'
     trailingSlash: false

   metadata:
     title:
       default: 'campivacorp.'
       template: '%s -- campivacorp.'
     description: 'campivacorp. - Soluciones agroindustriales para el mundo. Trading, brokeraje y logistica de productos agroindustriales con certificaciones internacionales.'
     robots:
       index: true
       follow: true
     openGraph:
       site_name: 'campivacorp.'
       images:
         - url: '~/assets/images/default.png'
           width: 1200
           height: 628
       type: website

   i18n:
     language: es
     textDirection: ltr

   apps:
     blog:
       isEnabled: false
       postsPerPage: 6
       post:
         isEnabled: false
         permalink: '/%slug%'
         robots:
           index: false
       list:
         isEnabled: false
         pathname: 'blog'
         robots:
           index: false
       category:
         isEnabled: false
         pathname: 'category'
         robots:
           index: false
       tag:
         isEnabled: false
         pathname: 'tag'
         robots:
           index: false
       isRelatedPostsEnabled: false
       relatedPostsCount: 4

   ui:
     theme: 'light:only'
   ```
   Key changes: Remove googleSiteVerificationId, remove twitter block, remove analytics block, set all blog options to false, language to es, theme to light:only.

3. DELETE the entire src/pages/[...blog]/ directory (index.astro, [...page].astro, [category]/, [tag]/).
   Do NOT delete src/utils/blog.ts or src/components/blog/ -- they may still be referenced by widget components.
   Do NOT delete src/content/config.ts -- harmless and may cause build errors if removed.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CustomStyles.astro has campivacorp. green palette and Nunito Sans/Montserrat fonts. config.yaml shows "campivacorp." title, Spanish language, blog disabled, light:only theme. Blog route directory deleted. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "daisyui" node_modules/.package-lock.json` returns nothing (DaisyUI fully removed)
3. `grep "Inter" src/components/CustomStyles.astro` returns nothing (old font gone)
4. `grep "rgb(1 97 239)" src/components/CustomStyles.astro` returns nothing (old blue gone)
5. `grep "campivacorp" src/config.yaml` returns matches (new brand name present)
6. `ls src/pages/[...blog]/` fails (directory deleted)
</verification>

<success_criteria>
- npm run build succeeds
- No DaisyUI in package.json or tailwind.config.js
- CustomStyles.astro contains only campivacorp. brand values (green palette, Nunito Sans, Montserrat)
- config.yaml has site name "campivacorp.", language "es", blog disabled, theme "light:only"
- Blog route files are deleted
- No Inter font references remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
