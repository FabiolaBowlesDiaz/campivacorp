---
phase: 02-page-shell-above-the-fold
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, HERO-01, HERO-02, HERO-03, HERO-04, HERO-05, HERO-06, STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "Visiting the site shows hero slider immediately -- no AstroWind demo content visible"
    - "Scrolling down past hero shows animated stat counters on dark background"
    - "Navbar anchor links point to section ids that exist on the page"
    - "Production build succeeds and Swiper initializes correctly"
    - "Mobile hamburger menu works and CTA button is visible"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Single-page layout composing HeroSlider, StatsCounter, and placeholder sections"
      contains: "HeroSlider"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/widgets/HeroSlider.astro"
      via: "Astro component import"
      pattern: "import HeroSlider"
    - from: "src/pages/index.astro"
      to: "src/components/widgets/StatsCounter.astro"
      via: "Astro component import"
      pattern: "import StatsCounter"
    - from: "src/navigation.ts"
      to: "src/pages/index.astro"
      via: "anchor href matches section id"
      pattern: "#nosotros.*id=\"nosotros\""
---

<objective>
Rewrite index.astro as the single-page landing, composing HeroSlider + StatsCounter + placeholder sections, then verify the complete above-the-fold experience.

Purpose: This is the integration plan -- wiring the navigation data (Plan 01) and widget components (Plan 02) into a working page. The checkpoint ensures the first impression is correct before proceeding to content sections.

Output: Rewritten index.astro and verified working page.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-shell-above-the-fold/02-RESEARCH.md
@.planning/phases/02-page-shell-above-the-fold/02-01-SUMMARY.md
@.planning/phases/02-page-shell-above-the-fold/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (navigation.ts after rewrite): -->
headerData.links = [
  { text: 'Inicio', href: '#inicio' },
  { text: 'Nosotros', href: '#nosotros' },
  { text: 'Productos', href: '#productos' },
  { text: 'Servicios', href: '#servicios' },
  { text: 'Valores', href: '#valores' },
  { text: 'Certificaciones', href: '#certificaciones' },
  { text: 'Contacto', href: '#contacto' },
]

<!-- From Plan 02 (new components): -->
HeroSlider.astro: renders <section id="inicio"> with Swiper slider (no props needed)
StatsCounter.astro: renders <section id="stats"> with animated counters (no props needed)

<!-- WidgetWrapper scroll offset for anchor sections: -->
class="scroll-mt-[72px]" (accounts for sticky header height)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.astro as single-page landing with section anchors</name>
  <files>src/pages/index.astro</files>
  <action>
Completely rewrite src/pages/index.astro. Remove ALL AstroWind demo widget imports (Hero, Note, Features, Features2, Steps, Content, BlogLatestPosts, FAQs, Stats, CallToAction) and their usage.

New file content:

**Frontmatter:**
```astro
---
import Layout from '~/layouts/PageLayout.astro';
import HeroSlider from '~/components/widgets/HeroSlider.astro';
import StatsCounter from '~/components/widgets/StatsCounter.astro';

const metadata = {
  title: 'campivacorp. - Soluciones agroindustriales para el mundo',
  ignoreTitleTemplate: true,
};
---
```

**Template:**
```astro
<Layout metadata={metadata}>
  <!-- Hero Slider (id="inicio" is inside the component) -->
  <HeroSlider />

  <!-- Stats Counter (id="stats" is inside the component) -->
  <StatsCounter />

  <!-- Phase 3 placeholder sections -->
  <section id="nosotros" class="scroll-mt-[72px] py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Quienes Somos -- Phase 3</p>
    </div>
  </section>

  <section id="productos" class="scroll-mt-[72px] py-16 md:py-20 bg-gray-50">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Productos -- Phase 3</p>
    </div>
  </section>

  <section id="servicios" class="scroll-mt-[72px] py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Servicios -- Phase 3</p>
    </div>
  </section>

  <section id="valores" class="scroll-mt-[72px] py-16 md:py-20 bg-gray-50">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Valores -- Phase 3</p>
    </div>
  </section>

  <section id="certificaciones" class="scroll-mt-[72px] py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Certificaciones -- Phase 3</p>
    </div>
  </section>

  <section id="contacto" class="scroll-mt-[72px] py-16 md:py-20 bg-gray-50">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-400">
      <p class="text-sm uppercase tracking-wider">Contacto -- Phase 4</p>
    </div>
  </section>
</Layout>
```

**Key details:**
- Every section id matches an anchor href in navigation.ts (this is the nav-to-content wiring)
- scroll-mt-[72px] on each placeholder accounts for the sticky header height
- Alternating bg-gray-50 on placeholders provides visual separation while scrolling
- Placeholder text is intentionally minimal -- Phase 3 and 4 will replace these entirely
- The metadata title uses "campivacorp." branding, not "AstroWind"
- ignoreTitleTemplate: true prevents the site name from being appended twice

**CRITICAL:** Do NOT import any AstroWind demo widgets. The only widget imports are HeroSlider and StatsCounter.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>index.astro imports only HeroSlider + StatsCounter, has placeholder sections with matching anchor ids for all 7 nav links, metadata title shows "campivacorp.", build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete above-the-fold experience</name>
  <files>src/pages/index.astro</files>
  <action>
Run `npm run dev` to start the dev server. Then run `npm run build` and `npm run preview` to verify the production build. Present the verification checklist to the user for visual confirmation of all Phase 2 deliverables.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: navbar shows logo + flat links + green CTA, hero slider cycles 3 slides with fade, stats count up on scroll, all nav anchors work, mobile hamburger works, production build Swiper initializes</done>
  <what-built>
Complete Phase 2 above-the-fold experience:
- Navbar with campivacorp. logo, 7 anchor links, green "Contactanos" CTA
- Hero slider with 3 gradient slides, fade transitions, autoplay, pagination dots
- Stats counter section with animated numbers on dark background
- Single-page layout with placeholder sections for all nav anchors
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321
2. **Navbar check:**
   - Logo (PNG isotipo + "campivacorp." text) visible top-left
   - 7 flat text links visible (no dropdowns on hover)
   - Green "Contactanos" button visible top-right
   - Scroll down -- navbar should transition from transparent to white with shadow
3. **Hero slider check:**
   - Full-viewport gradient background with dark overlay visible
   - White heading text centered ("Soluciones agroindustriales para el mundo")
   - Two CTA buttons below: green "Ver Productos" + white outline "Contactar"
   - Wait ~5 seconds -- slide should fade to next ("Calidad certificada...")
   - White pagination dots at bottom, clickable
   - On desktop: left/right navigation arrows visible (hidden on mobile viewport)
4. **Stats check:**
   - Scroll past hero to dark (#25272f) section
   - 4 numbers should animate counting up from 0: 25+, 7, 5, 100%
   - Labels below each number in smaller white/muted text
   - Animation should fire once only (scroll away and back -- no re-animation)
5. **Navigation check:**
   - Click "Productos" in navbar -- page scrolls to the "Productos -- Phase 3" placeholder
   - Click "Contacto" -- page scrolls to the "Contacto -- Phase 4" placeholder
   - Verify scroll offset -- section content should not be hidden behind sticky header
6. **Mobile check (resize browser to ~375px):**
   - Hamburger icon visible, click it -- menu opens with all 7 links
   - Click a link -- menu closes and page scrolls to section
   - Hero text scales down appropriately
   - Stats grid shows 2x2 layout
7. **Production build:** Run `npm run build && npm run preview` -- verify Swiper still works (not just dev mode)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds (no build errors)
- `npm run preview` shows working Swiper slider (production build test)
- All 7 navbar anchor links scroll to matching section ids
- No AstroWind demo content visible anywhere on the page
- Browser tab shows "campivacorp. - Soluciones agroindustriales para el mundo"
</verification>

<success_criteria>
- Visitor sees professional hero slider with branded messaging immediately on page load
- Scrolling reveals animated stat counters on dark background
- All navbar links work (smooth scroll to correct sections)
- Mobile experience is functional (hamburger menu, responsive layout)
- Production build works correctly (Swiper initializes, not just dev mode)
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-shell-above-the-fold/02-03-SUMMARY.md`
</output>
