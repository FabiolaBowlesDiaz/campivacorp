---
phase: 02-page-shell-above-the-fold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/widgets/HeroSlider.astro
  - src/components/widgets/StatsCounter.astro
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, HERO-05, HERO-06, STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "Hero slider displays 3 full-height slides with dark overlay and branded text"
    - "Each slide has dual CTAs: 'Ver Productos' (green) and 'Contactar' (white outline)"
    - "Swiper autoplay cycles slides every 5 seconds with fade transitions"
    - "Pagination dots are white and clickable, navigation arrows appear on desktop only"
    - "Stats section shows 4 counters that animate from 0 when scrolled into view"
  artifacts:
    - path: "src/components/widgets/HeroSlider.astro"
      provides: "Swiper-based hero slider with 3 branded slides"
      min_lines: 80
      contains: "swiper-slide"
    - path: "src/components/widgets/StatsCounter.astro"
      provides: "Animated counter section with IntersectionObserver"
      min_lines: 50
      contains: "data-target"
  key_links:
    - from: "src/components/widgets/HeroSlider.astro"
      to: "swiper"
      via: "script tag import"
      pattern: "import Swiper from 'swiper'"
    - from: "src/components/widgets/StatsCounter.astro"
      to: "IntersectionObserver"
      via: "script tag"
      pattern: "IntersectionObserver"
---

<objective>
Create the HeroSlider and StatsCounter widget components -- the two major visual elements of the above-the-fold experience.

Purpose: These are the first things a visitor sees. The hero slider establishes campivacorp.'s identity with branded messaging and CTAs. The stats section builds credibility with animated numbers.

Output: Two new Astro components ready to be imported by index.astro (Plan 03).
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-shell-above-the-fold/02-RESEARCH.md

<interfaces>
<!-- Swiper CSS is already imported globally in Layout.astro (core, navigation, pagination, effect-fade after Plan 01) -->
<!-- Swiper 12.1.2 is installed in node_modules -->

From src/layouts/Layout.astro (CSS imports, lines 2-7 after Plan 01):
```astro
import '~/assets/styles/tailwind.css';
import 'aos/dist/aos.css';
import 'swiper/css';
import 'swiper/css/navigation';
import 'swiper/css/pagination';
import 'swiper/css/effect-fade';
```

From src/layouts/Layout.astro (View Transitions + AOS pattern, lines 53-67):
```astro
<script>
  import AOS from 'aos';
  function initAOS() { AOS.init({ duration: 800, easing: 'ease-in-out', once: true, offset: 50 }); }
  initAOS();
  document.addEventListener('astro:after-swap', initAOS);
</script>
```

Brand tokens available via CSS variables (from Phase 1 CustomStyles.astro):
- --aw-color-primary: rgb(149 180 68) = #95b444
- --aw-color-secondary: rgb(93 111 49) = #5d6f31
- --aw-color-accent: rgb(203 220 83) = #cbdc53
- font-heading: 'Nunito Sans', sans-serif (weight 800)
- font-sans: 'Montserrat', sans-serif (weights 500, 700)

Tailwind classes: bg-primary, text-primary, bg-secondary, font-heading, font-sans
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSlider.astro with Swiper fade, 3 slides, dual CTAs</name>
  <files>src/components/widgets/HeroSlider.astro</files>
  <action>
Create src/components/widgets/HeroSlider.astro as a new file.

**HTML structure:**
- Outer wrapper: `<section id="inicio">` (no WidgetWrapper -- hero needs full viewport, no padding/max-width)
- Swiper container: `<div class="swiper hero-swiper">` with `<div class="swiper-wrapper">` inside
- 3 `<div class="swiper-slide relative">` elements, each containing:
  1. Background gradient div (absolute, inset-0): use distinct gradients as placeholders:
     - Slide 1: `bg-gradient-to-br from-[#25272f] via-[#3a4020] to-[#5d6f31]` (agricultural fields feel)
     - Slide 2: `bg-gradient-to-br from-[#25272f] to-[#2a3518]` (products/processing)
     - Slide 3: `bg-gradient-to-br from-[#1a1c23] to-[#25272f]` (global commerce)
  2. Overlay div: `<div class="absolute inset-0 bg-[#25272f]/60"></div>`
  3. Content div: `<div class="relative z-10 flex flex-col items-center justify-center min-h-screen text-center px-4">`
     - Heading: `<h2 class="font-heading font-extrabold text-white text-3xl md:text-5xl lg:text-6xl mb-4 max-w-4xl">`
     - Subheading: `<p class="font-sans text-white/80 text-lg md:text-xl mb-8 max-w-2xl">`
     - CTA container: `<div class="flex flex-col sm:flex-row gap-4">`
       - Primary: `<a href="#productos" class="bg-[#95b444] hover:bg-[#5d6f31] text-white font-bold px-8 py-3 rounded-md transition-colors">Ver Productos</a>`
       - Secondary: `<a href="#contacto" class="border-2 border-white text-white hover:bg-white/10 font-bold px-8 py-3 rounded-md transition-colors">Contactar</a>`
- After swiper-wrapper, add pagination and navigation elements:
  - `<div class="swiper-pagination"></div>`
  - `<div class="swiper-button-prev"></div>`
  - `<div class="swiper-button-next"></div>`

**Slide content (exact text per locked decision):**
- Slide 1: heading "Soluciones agroindustriales para el mundo", sub "Trading, brokeraje y logistica de productos agroindustriales"
- Slide 2: heading "Calidad certificada en cada transaccion", sub "Estendares internacionales que garantizan la excelencia"
- Slide 3: heading "25 anos conectando mercados", sub "Experiencia y confianza en el comercio agroindustrial"

**Script tag (client-side Swiper init):**
```html
<script>
  import Swiper from 'swiper';
  import { Navigation, Pagination, Autoplay, EffectFade } from 'swiper/modules';

  let heroSwiperInstance: Swiper | null = null;

  function initHeroSwiper() {
    const el = document.querySelector('.hero-swiper');
    if (!el) return;

    // Destroy previous instance if exists (View Transitions cleanup)
    if (heroSwiperInstance) {
      heroSwiperInstance.destroy(true, true);
      heroSwiperInstance = null;
    }

    heroSwiperInstance = new Swiper('.hero-swiper', {
      modules: [Navigation, Pagination, Autoplay, EffectFade],
      effect: 'fade',
      fadeEffect: { crossFade: true },
      autoplay: { delay: 5000, disableOnInteraction: false },
      pagination: { el: '.swiper-pagination', clickable: true },
      navigation: { nextEl: '.swiper-button-next', prevEl: '.swiper-button-prev' },
      loop: true,
    });
  }

  initHeroSwiper();
  document.addEventListener('astro:after-swap', initHeroSwiper);
</script>
```

**Style block (scoped global for Swiper overrides):**
```html
<style is:global>
  .hero-swiper .swiper-pagination-bullet {
    background: white;
    opacity: 0.5;
  }
  .hero-swiper .swiper-pagination-bullet-active {
    opacity: 1;
  }
  .hero-swiper .swiper-button-prev,
  .hero-swiper .swiper-button-next {
    color: white;
    display: none;
  }
  @media (min-width: 768px) {
    .hero-swiper .swiper-button-prev,
    .hero-swiper .swiper-button-next {
      display: flex;
    }
  }
</style>
```

**CRITICAL anti-patterns to avoid:**
- Do NOT import Swiper in the Astro frontmatter (---) block. It must be in the `<script>` tag.
- Do NOT use AOS attributes inside this component. AOS and Swiper conflict on transforms.
- Do NOT wrap in WidgetWrapper. Hero needs full viewport height without padding constraints.
- Do NOT forget `astro:after-swap` listener. ClientRouter (View Transitions) is active.
- Do NOT forget `fadeEffect: { crossFade: true }` -- without it, slides stack visibly.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>HeroSlider.astro exists with 3 slides (correct text), Swiper fade+autoplay+pagination+navigation config in script tag, white pagination dots, desktop-only arrows, gradient backgrounds, dual CTAs on each slide</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsCounter.astro with IntersectionObserver count-up animation</name>
  <files>src/components/widgets/StatsCounter.astro</files>
  <action>
Create src/components/widgets/StatsCounter.astro as a new file.

**HTML structure:**
- Section: `<section id="stats" class="bg-[#25272f] py-16 md:py-20">`
- Container: `<div class="max-w-6xl mx-auto px-4">`
- Grid: `<div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">`
- 4 stat items, each:
  ```html
  <div>
    <div class="font-heading font-extrabold text-white text-4xl md:text-5xl" data-target="25" data-suffix="+">0</div>
    <div class="font-sans font-medium text-white/70 text-sm mt-2 uppercase tracking-wider">Anos de experiencia</div>
  </div>
  ```

**4 stats (per locked decision):**
1. target="25", suffix="+", label "Anos de experiencia"
2. target="7", suffix="", label "Categorias de productos"
3. target="5", suffix="", label "Certificaciones internacionales"
4. target="100", suffix="%", label "Compromiso con la calidad"

**Script tag (IntersectionObserver counter animation):**
```html
<script>
  function animateCounters() {
    const section = document.getElementById('stats');
    if (!section) return;

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const counters = section.querySelectorAll('[data-target]');
          counters.forEach(counter => {
            const target = parseFloat(counter.getAttribute('data-target') || '0');
            const suffix = counter.getAttribute('data-suffix') || '';
            const duration = 2000;
            const startTime = performance.now();

            function update(currentTime: number) {
              const elapsed = currentTime - startTime;
              const progress = Math.min(elapsed / duration, 1);
              // easeOutQuart for natural deceleration
              const eased = 1 - Math.pow(1 - progress, 4);
              const current = Math.floor(eased * target);
              counter.textContent = current + suffix;
              if (progress < 1) requestAnimationFrame(update);
            }
            requestAnimationFrame(update);
          });
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.3 });

    observer.observe(section);
  }

  animateCounters();
  document.addEventListener('astro:after-swap', animateCounters);
</script>
```

**CRITICAL:**
- Do NOT use AOS for the counter animation trigger. Use IntersectionObserver directly (per locked decision).
- Do NOT forget `observer.unobserve(entry.target)` after first trigger -- prevents re-animation on scroll up/down.
- Do NOT forget `astro:after-swap` listener for View Transitions re-initialization.
- The section id="stats" is used both for the IntersectionObserver query AND as a potential anchor target.
  </action>
  <verify>
    <automated>cd "C:\Users\Lenovo\OneDrive - theblankinc.com\IA Consulting\campiva corp\Landing Page\campivacorp" && npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>StatsCounter.astro exists with 4 stat items (25+, 7, 5, 100%), dark bg #25272f, IntersectionObserver-triggered count-up animation with easeOutQuart easing over 2s, fires once only, responsive 2-col mobile / 4-col desktop grid</done>
</task>

</tasks>

<verification>
- `npx astro check` reports no type errors in either new component
- HeroSlider.astro has exactly 3 swiper-slide elements with correct heading text
- HeroSlider.astro script tag imports Swiper + modules (Navigation, Pagination, Autoplay, EffectFade)
- HeroSlider.astro has `astro:after-swap` listener and previous instance cleanup
- StatsCounter.astro has 4 data-target elements (25, 7, 5, 100)
- StatsCounter.astro uses IntersectionObserver (not AOS) with observer.unobserve
- Neither component uses AOS attributes
- Both components have `astro:after-swap` re-initialization
</verification>

<success_criteria>
- HeroSlider component renders 3 full-viewport slides with gradient backgrounds, dark overlay, branded headings, and dual CTAs
- Swiper initializes with fade effect, 5s autoplay, clickable pagination, desktop-only arrows
- StatsCounter renders 4 stats on dark background with count-up animation triggered by viewport entry
- Both components are standalone -- can be imported by any page
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-shell-above-the-fold/02-02-SUMMARY.md`
</output>
